---
description: 
globs: 
alwaysApply: false
---
编写一个前后端分离的二手手机交易与回收 Web 应用，分为用户端和管理端两个界面，具体要求如下：

【整体要求】
- 使用 React + TailwindCSS 实现前端，分为用户端和管理端
- 使用 Node.js + Express 编写后端，所有用户提交的数据可以被管理端看到
- 所有数据使用MongoDB
- 图片上传支持本地存储即可
- 项目结构清晰，按模块组织代码（如 pages, components, services）

【用户端功能需求】
页面底部有三个固定导航栏按钮：
1. **首页**
   - 显示各类二手手机商品
   - 点击某个手机商品，跳转详情页
   - 商品详情页有「下单按钮」，点击后自动复制商品标题并跳转到一个预留的微信公众号（如跳转链接为 https://weixin.qq.com/）
2. **回收**
   - 显示回收流程（可为图片或文字说明）
   - 用户上传要回收的手机照片 + 填写信息（如品牌、型号等）
   - 点击“提交订单”后生成一条订单记录，后台可见
3. **我的**
   - 包括：注册登录（只需手机号+密码，无验证码）
   - 查看我的订单、浏览记录、收藏记录（可为空或模拟数据）

【管理端功能需求】
左侧为菜单栏，包括以下功能：
1. 控制台：欢迎语 + 简要统计（如商品数、订单数）
2. 用户管理：展示所有用户的注册信息
3. 订单管理：展示用户提交的回收订单（同步显示用户端上传的内容）

【后端 API 要求】
- 实现注册、登录接口（无需验证码）
- 实现获取商品列表、提交订单、获取订单列表、获取用户信息等接口
- 所有接口需支持 CORS，便于前端联调

【输出格式要求】
- 请一步性输出完整的前后端项目结构，目录清晰
- 每个主要文件请用 markdown 代码块给出（如 App.jsx、server.js 等）
- 提供一个本地 mock 数据示例（如 phones.json, users.json, orders.json）
- 图片上传用 HTML file input 模拟，上传后文件保存在本地 /uploads 文件夹

我希望开发完可以部署在linux服务器上
请一次性输出所有da核心代码和结构，我希望完整复制即可使用，无需反复改代码，谢谢！










